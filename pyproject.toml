[project]
name = "englearner"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Avalon", email = "avalonliberty32@gmail.com" }
]
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.129.0",
    "httpx>=0.28.1",
    "pydantic>=2.12.5",
    "pytest>=9.0.2",
    "python-dotenv>=1.2.1",
    "python-jose>=3.5.0",
    "supabase>=2.28.0",
    "uvicorn>=0.40.0",
]

[build-system]
requires = ["uv_build>=0.9.18,<0.10.0"]
build-backend = "uv_build"
